<Window x:Class="AiDesk.App.Widgets.SystemWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AiDesk 小组件" Width="240" Height="150"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        MouseLeftButtonDown="OnDrag" Loaded="OnLoaded" Closing="OnClosing">

    <Border Background="#B314181C" CornerRadius="16" BorderBrush="#40FFFFFF" BorderThickness="1"
            Padding="18,14" MouseLeftButtonDown="OnDrag">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 时钟与日期 -->
            <StackPanel>
                <TextBlock x:Name="TimeText" Text="00:00" FontSize="44" FontWeight="Light"
                           Foreground="White" FontFamily="Segoe UI Light, Microsoft YaHei UI" />
                <TextBlock x:Name="DateText" Text="----年--月--日" FontSize="13" Foreground="#CCFFFFFF"
                           Margin="2,0,0,0" />
            </StackPanel>

            <!-- 系统监控 -->
            <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="2,10,0,0">
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="46" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="CPU" FontSize="12" Foreground="#B0FFFFFF" />
                    <TextBlock Grid.Column="1" x:Name="CpuText" Text="--%" FontSize="12" Foreground="White"
                               TextAlignment="Right" />
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="46" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="内存" FontSize="12" Foreground="#B0FFFFFF" />
                    <TextBlock Grid.Column="1" x:Name="MemText" Text="--%" FontSize="12" Foreground="White"
                               TextAlignment="Right" />
                </Grid>
            </StackPanel>

            <!-- 关闭按钮 -->
            <Button Grid.Row="1" Content="✕" Width="22" Height="22" FontSize="10"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="Transparent" Foreground="#8AFFFFFF" BorderThickness="0"
                    Cursor="Hand" Click="OnCloseClicked" ToolTip="关闭小组件" />
        </Grid>
    </Border>
</Window>
