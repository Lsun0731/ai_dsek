<Window x:Class="AiDesk.App.DesktopDock.SearchOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="搜索应用" Width="560" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False"
        ResizeMode="NoResize" SizeToContent="Height"
        KeyDown="OnWindowKeyDown" Deactivated="OnWindowDeactivated">
    <Border Background="#F2101522" CornerRadius="16" BorderBrush="#33FFFFFF" BorderThickness="1" Padding="16">
        <StackPanel>
            <TextBox x:Name="SearchBox" FontSize="18" Foreground="#E8ECF2" CaretBrush="#4C7CF3"
                     Background="Transparent" BorderThickness="0"
                     TextChanged="OnSearchTextChanged" KeyDown="OnSearchKeyDown" />
            <Separator Height="1" Background="#22FFFFFF" Margin="0,10,0,10" />
            <ListBox x:Name="SearchList" MaxHeight="300" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="OnSearchResultDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="📄" FontSize="15" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Name}" FontSize="14" Foreground="#E8ECF2"
                                       Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock x:Name="EmptyText" Text="输入应用名搜索，回车启动" FontSize="12"
                       Foreground="#6A7488" Visibility="Collapsed" />
        </StackPanel>
    </Border>
</Window>
